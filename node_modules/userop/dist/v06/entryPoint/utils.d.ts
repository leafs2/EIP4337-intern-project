import { Address } from "viem";
import { RawUserOperation, UserOperation } from "./types";
export declare const calculateUserOpHash: (userop: UserOperation, entryPoint: Address, chainId: number) => `0x${string}`;
export declare const toRawUserOperation: (userop: UserOperation) => RawUserOperation;
